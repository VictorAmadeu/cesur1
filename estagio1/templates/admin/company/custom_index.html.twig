{% extends '@EasyAdmin/crud/index.html.twig' %}

{% block content %}
    <div class="d-flex-col" style="display:flex-col;margin-top:20px;">
    {# <div>
        <div class="d-flex align-items-center justify-content-between">
        <form method="POST" id="account-form">
            <div class="form-group">
                {% if 'ROLE_ADMIN' not in app.user.roles %}
                <select name="account_id" id="account-select" class="form-select me-3" style="width: 250px;">
                    {% for account in accounts %}
                    <option value="{{ account.id }}" {% if selectedAccount and selectedAccount.id == account.id %} selected {% endif %}>
                        {{ account.name }}
                    </option>
                    {% endfor %}
                </select>
                {% else %}
                <span>{{ selectedAccount.name }}</span>
                <input type="hidden" name="account_id" value="{{ selectedAccount.id }}">
                {% endif %}
            </div>
        </form>
    </div> #}
    {# Renderizamos el CRUD por defecto de EasyAdmin #}
        {{ parent() }}
{% endblock %}
