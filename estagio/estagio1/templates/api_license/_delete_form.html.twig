<form method="post" action="{{ path('app_api_license_delete', {'id': license.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ license.id) }}">
    <button class="btn">Delete</button>
</form>
